---
phase: 01-implementation
plan: '03'
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaAnalyze.java
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
must_haves:
  truths:
    - Unit tests for DeltaAnalyzeProperties pass
    - Unit tests for DeltaMetadata analyze methods pass
    - Integration tests for ANALYZE statement work
    - Partition statistics tests work
  artifacts:
    - path: "presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaAnalyze.java"
      provides: "Comprehensive test coverage for ANALYZE functionality"
      min_lines: 150
---

<objective>
Create comprehensive unit and integration tests for the ANALYZE functionality. This validates that all requirements are met and catches regressions.
</objective>

<execution_context>
@/Users/jerry/my/presto/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jerry/my/presto/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-implementation/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@presto-delta/src/test/java/com/facebook/presto/delta
</context>

<tasks>
<task type="auto">
<name>Task 1: Create TestDeltaAnalyzeProperties unit tests</name>
<files>presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaAnalyzeProperties.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaAnalyzeProperties.java
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaTableProperties.java (for test pattern)
</read_first>
<action>
Create TestDeltaAnalyzeProperties.java following presto-hive/TestHiveAnalyzeProperties pattern:

1. Test class setup:
   - Extend BaseDeltaTest or similar test base
   - Use TestNG framework (as per AGENTS.md)
   - Inject mock TypeManager

2. Test cases for TEST-01:
   - testGetPropertiesReturnsNonEmptyList: Verify getProperties() returns non-empty list
   - testPartitionColumnsProperty: Verify "partition_columns" property exists
   - testSamplePercentageProperty: Verify "sample_percentage" property exists
   - testPropertyTypes: Verify correct types for each property
   - testPropertyDefaults: Verify default values are sensible

3. Test for complex type handling:
   - Verify configuration accepts column lists
   - Verify validation of input parameters
</action>
<verify>
<automated>ls -la presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaAnalyzeProperties.java 2>/dev/null && echo "FILE_EXISTS"</automated>
</verify>
<done>TestDeltaAnalyzeProperties.java created and tests pass</done>
</task>

<task type="auto">
<name>Task 2: Create TestDeltaMetadataAnalyze unit tests</name>
<files>presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaMetadataAnalyze.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java
- presto-delta/src/test/java/com/facebook/presto/delta
</read_first>
<action>
Create TestDeltaMetadataAnalyze.java for DeltaMetadata analyze methods:

1. Test class setup:
   - Mock DeltaClient and ExtendedHiveMetastore
   - Set up test Delta tables (partitioned and non-partitioned)

2. Test cases for TEST-02:
   - testGetTableHandleForStatisticsCollection: Verify method returns valid handle
   - testStatisticsCollectionWithPartitions: Test partition-specific analysis
   - testStatisticsCollectionWithoutPartitions: Test non-partitioned table analysis
   - testStatisticsStorageInTableParameters: Verify stats stored in metastore

3. Edge cases:
   - Test handling of complex columns (should skip, not fail)
   - Test error handling for inaccessible tables
   - Test with empty table
</action>
<verify>
<automated>ls -la presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaMetadataAnalyze.java 2>/dev/null && echo "FILE_EXISTS"</automated>
</verify>
<done>TestDeltaMetadataAnalyze.java created</done>
</task>

<task type="auto">
<name>Task 3: Create integration test TestDeltaAnalyze</name>
<files>presto-delta/src/test/java/com/facebook/presto/delta/TestDeltaAnalyze.java</files>
<read_first>
- presto-delta/src/test/java/com/facebook/presto/delta
</read_first>
<action>
Create TestDeltaAnalyze.java as the main integration test (TEST-03, TEST-04):

1. Follow presto-hive/TestHiveAnalyze.java pattern
2. Test setup:
   - Create test Delta tables with known data
   - Use TestingPrestoSession or similar test harness

3. Test cases:
   - TEST-03: testAnalyzeTableStatement: Run actual ANALYZE TABLE statement
   - TEST-04: testAnalyzePartitionedTable: Test partition-specific analyze with WITH (partitions = ...)
   - testAnalyzeNonPartitionedTable: Test PART-03
   - testStatisticsStoredCorrectly: Verify min/max/nulls stored properly
   - testAnalyzeComplexTypeColumns: Verify complex types are skipped gracefully
   - testMultipleAnalyzes: Run ANALYZE multiple times, verify stats update

4. Test data:
   - Create small Delta tables with known min/max/null values
   - Verify collected statistics match expected values
</action>
<verify>
<automated>./mvnw test -pl presto-delta -Dtest=TestDeltaAnalyze -DskipUI -q 2>&1 | tail -20</automated>
</verify>
<done>TestDeltaAnalyze integration tests pass</done>
</task>

<task type="auto">
<name>Task 4: Run all tests and verify coverage</name>
<files>presto-delta/src/test/java/com/facebook/presto/delta</files>
<read_first>
- presto-delta/pom.xml
</read_first>
<action>
Verify test coverage meets requirement (TEST-01 requirement: 80%+ coverage):

1. Run all new tests:
   ./mvnw test -pl presto-delta -Dtest="TestDeltaAnalyze*" -DskipUI

2. Check code coverage:
   - Ensure DeltaAnalyzeProperties: 80%+ coverage
   - Ensure DeltaMetadata analyze-related methods: 80%+ coverage
   - If coverage is below threshold, add more test cases

3. Verify all tests pass:
   - Fix any failing tests
   - Ensure test infrastructure is correct
</action>
<verify>
<automated>./mvnw test -pl presto-delta -Dtest="TestDeltaAnalyze*" -DskipUI 2>&1 | grep -E "Tests run:|BUILD"</automated>
</verify>
<done>All ANALYZE-related tests pass with 80%+ coverage</done>
</task>
</tasks>

<verification>
- [ ] TestDeltaAnalyzeProperties.java exists and passes
- [ ] TestDeltaMetadataAnalyze.java exists and passes
- [ ] TestDeltaAnalyze.java (integration) exists and passes
- [ ] All tests pass: ./mvnw test -pl presto-delta -DskipUI
- [ ] Code coverage: 80%+ for new components
</verification>

<success_criteria>
All unit tests and integration tests pass. ANALYZE TABLE statement works end-to-end. Partition-specific analyze with WITH (partitions = ...) works correctly. 80%+ code coverage achieved on new components.
</success_criteria>

<output>
After completion, create `.planning/phases/01-implementation/01-03-SUMMARY.md`
</output>