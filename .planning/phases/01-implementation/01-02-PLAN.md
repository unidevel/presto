---
phase: 01-implementation
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java
autonomous: true
requirements:
  - CONN-02
  - ANALYZE-01
  - ANALYZE-02
  - PART-01
  - PART-02
  - PART-03
must_haves:
  truths:
    - DeltaMetadata implements getTableHandleForStatisticsCollection()
    - Statistics (min, max, null count) are stored in table metadata
    - Partition-specific analyze works with WITH (partitions = ...)
    - Non-partitioned tables work correctly
  artifacts:
    - path: "presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java"
      provides: "getTableHandleForStatisticsCollection implementation"
      contains: "getTableHandleForStatisticsCollection"
---

<objective>
Implement getTableHandleForStatisticsCollection() in DeltaMetadata and statistics collection/storage logic. This enables the core ANALYZE functionality.
</objective>

<execution_context>
@/Users/jerry/my/presto/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jerry/my/presto/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-implementation/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java
</context>

<tasks>
<task type="auto">
<name>Task 1: Implement getTableHandleForStatisticsCollection in DeltaMetadata</name>
<files>presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java
</read_first>
<action>
Add getTableHandleForStatisticsCollection() method to DeltaMetadata.java:

1. Import required types:
   - ConnectorTableHandle (already imported)
   - StatisticsCollectionType (from presto-spi)
   - ConnectorTableHandle (from presto-spi)

2. Method signature (follow HiveMetadata.java line 577):
   @Override
   public ConnectorTableHandle getTableHandleForStatisticsCollection(
       ConnectorSession session, 
       ConnectorTableHandle tableHandle, 
       StatisticsCollectionType statisticsCollectionType)

3. Implementation:
   - Cast tableHandle to DeltaTableHandle
   - Check connectorId matches (re-use checkConnectorId method)
   - Return the same DeltaTableHandle (statistics collection happens on existing handle)
   - Handle case where statisticsCollectionType indicates full collection vs incremental

4. Handle partition filtering:
   - Extract partition filter from statisticsCollectionType
   - For PART-01 (specific partitions): Only analyze specified partitions
   - For PART-02 (partition stats): Collect partition-level statistics
   - For PART-03 (non-partitioned): Handle tables without partition columns
</action>
<verify>
<automated>grep -n "getTableHandleForStatisticsCollection" presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java</automated>
</verify>
<done>DeltaMetadata.getTableHandleForStatisticsCollection() implemented</done>
</task>

<task type="auto">
<name>Task 2: Implement statistics collection logic</name>
<files>presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaTable.java
</read_first>
<action>
Implement the actual statistics gathering and storage logic:

1. Create internal method to collect column statistics:
   - Scan Delta table data to compute min, max, null count for each column
   - Skip complex types (ARRAY, STRUCT, MAP) - log warning at INFO level
   - Use DeltaClient to read data and compute statistics

2. Create internal method to store statistics in Hive Metastore:
   - Use HiveMetastore.updateTable() to store statistics in table parameters
   - Follow presto-hive statistics parameter naming:
     - "presto.statistics.[column].min"
     - "presto.statistics.[column].max"
     - "presto.statistics.[column].nulls"
   - Store as strings, serialized appropriately for each type

3. Handle partitions (PART-01, PART-02):
   - If partition specification provided, only scan those partitions
   - Store partition-level statistics in partition metadata

4. Handle non-partitioned tables (PART-03):
   - Full table scan when no partition columns exist
   - Store statistics at table level
</action>
<verify>
<automated>grep -n "presto.statistics" presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java</automated>
</verify>
<done>Statistics collection and storage implemented in DeltaMetadata</done>
</task>

<task type="auto">
<name>Task 3: Wire up the complete analyze flow</name>
<files>presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaMetadata.java
</read_first>
<action>
Complete the analyze flow integration:

1. In getTableHandleForStatisticsCollection, after returning the handle:
   - The statistics collection is triggered by the Presto optimizer
   - Ensure the table handle includes all necessary information for statistics collection

2. Add any additional imports needed:
   - StatisticsCollectionType from com.facebook.presto.spi.statistics
   - Ensure proper error handling for Delta table access issues

3. Add error handling:
   - Handle case where Delta table cannot be accessed
   - Handle case where statistics collection fails mid-way
   - Ensure partial results don't corrupt existing statistics
</action>
<verify>
<automated>./mvnw compile -pl presto-delta -DskipTests -q 2>&1 | head -20</automated>
</verify>
<done>DeltaMetadata compiles with new methods</done>
</task>
</tasks>

<verification>
- [ ] getTableHandleForStatisticsCollection method implemented
- [ ] Statistics stored in table metadata (Hive Metastore parameters)
- [ ] Partition filtering works for PART-01, PART-02
- [ ] Non-partitioned table support works for PART-03
- [ ] Complex types are skipped with warning
- [ ] Build succeeds: ./mvnw compile -pl presto-delta -DskipTests
</verification>

<success_criteria>
User can run ANALYZE TABLE on a Delta table and column statistics (min, max, null count) are stored in the table metadata. Partition-specific analyze with WITH (partitions = ...) works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-implementation/01-02-SUMMARY.md`
</output>