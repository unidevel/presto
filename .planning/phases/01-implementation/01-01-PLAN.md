---
phase: 01-implementation
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - presto-delta/src/main/java/com/facebook/presto/delta/DeltaConnector.java
autonomous: true
requirements:
  - CONN-01
must_haves:
  truths:
    - DeltaConnector exposes analyze properties via getAnalyzeProperties()
    - Users can run ANALYZE TABLE with column statistics collection options
  artifacts:
    - path: "presto-delta/src/main/java/com/facebook/presto/delta/DeltaAnalyzeProperties.java"
      provides: "Analyze properties definition following presto-hive pattern"
      min_lines: 50
    - path: "presto-delta/src/main/java/com/facebook/presto/delta/DeltaConnector.java"
      provides: "getAnalyzeProperties() method"
      contains: "getAnalyzeProperties"
---

<objective>
Create DeltaAnalyzeProperties class and integrate with DeltaConnector. This establishes the foundation for ANALYZE statement support.
</objective>

<execution_context>
@/Users/jerry/my/presto/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/jerry/my/presto/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-implementation/01-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@presto-delta/src/main/java/com/facebook/presto/delta/DeltaConnector.java
@presto-delta/src/main/java/com/facebook/presto/delta/DeltaTableProperties.java
</context>

<tasks>
<task type="auto">
<name>Task 1: Create DeltaAnalyzeProperties class</name>
<files>presto-delta/src/main/java/com/facebook/presto/delta/DeltaAnalyzeProperties.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaTableProperties.java (to understand PropertyMetadata pattern)
</read_first>
<action>
Create DeltaAnalyzeProperties.java following the pattern in presto-hive's HiveAnalyzeProperties.java:
1. Define column properties for ANALYZE (partition column filter, sample percentage)
2. Return List<PropertyMetadata<?>> from getProperties()
3. Use the same property names as presto-hive for compatibility:
   - "partition_columns" - list of partition columns to analyze
   - "sample_percentage" - sample percentage for statistics
4. Handle skip for complex types (ARRAY, STRUCT, MAP) - log warning, continue with remaining columns
5. Statistics to collect: min, max, null count (no NDV for v1)

Key imports needed:
- com.facebook.presto.spi.session.PropertyMetadata
- com.facebook.presto.spi.type.TypeManager
- java.util.List
</action>
<verify>
<automated>grep -l "DeltaAnalyzeProperties" presto-delta/src/main/java/com/facebook/presto/delta/*.java</automated>
</verify>
<done>DeltaAnalyzeProperties.java exists with getProperties() method returning PropertyMetadata list</done>
</task>

<task type="auto">
<name>Task 2: Integrate DeltaAnalyzeProperties with DeltaConnector</name>
<files>presto-delta/src/main/java/com/facebook/presto/delta/DeltaConnector.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaConnector.java
</read_first>
<action>
Modify DeltaConnector.java to expose analyze properties:
1. Add DeltaAnalyzeProperties field (similar to deltaTableProperties)
2. Add to constructor injection
3. Implement getAnalyzeProperties() method that returns deltaAnalyzeProperties.getProperties()
4. Method signature: public List<PropertyMetadata<?>> getAnalyzeProperties()

Reference presto-hive/HiveConnector.java line 176 for exact method signature.
</action>
<verify>
<automated>grep -n "getAnalyzeProperties" presto-delta/src/main/java/com/facebook/presto/delta/DeltaConnector.java</automated>
</verify>
<done>DeltaConnector.getAnalyzeProperties() returns list of PropertyMetadata</done>
</task>

<task type="auto">
<name>Task 3: Register DeltaAnalyzeProperties in DeltaModule</name>
<files>presto-delta/src/main/java/com/facebook/presto/delta/DeltaModule.java</files>
<read_first>
- presto-delta/src/main/java/com/facebook/presto/delta/DeltaModule.java
</read_first>
<action>
Add DeltaAnalyzeProperties binding in DeltaModule.java:
1. Add import for DeltaAnalyzeProperties
2. Bind DeltaAnalyzeProperties with TypeManager dependency
3. Use binder.bind() pattern consistent with other bindings in DeltaModule
</action>
<verify>
<automated>grep -n "DeltaAnalyzeProperties" presto-delta/src/main/java/com/facebook/presto/delta/DeltaModule.java</automated>
</verify>
<done>DeltaAnalyzeProperties is bound in DeltaModule for dependency injection</done>
</task>
</tasks>

<verification>
- [ ] DeltaAnalyzeProperties.java compiles successfully
- [ ] DeltaConnector.getAnalyzeProperties() method exists
- [ ] Module binding is correct
- [ ] Build succeeds: ./mvnw compile -pl presto-delta -DskipTests
</verification>

<success_criteria>
DeltaConnector.getAnalyzeProperties() returns a valid list of PropertyMetadata objects that define the analyze options users can specify (partition_columns, sample_percentage).
</success_criteria>

<output>
After completion, create `.planning/phases/01-implementation/01-01-SUMMARY.md`
</output>